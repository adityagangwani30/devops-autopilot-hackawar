{
  "name": "agent-service",
  "version": "1.0.0",
  "description": "AI error analyzer agent service",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:rule": "node -e \"process.env.AGENT_MODE='rule'; require('./src/server.js')\"",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
